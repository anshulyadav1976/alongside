export default function HomePage() {
  return (
    <main className="shell">
      <p className="eyebrow">Local technical foundation</p>
      <h1>Someone steady, when you need a moment.</h1>
      <p className="lede">Alongside is a private, local hackathon demo for reflection, memory, and small next steps. Start a call from the product surface when it is connected.</p>
      <section className="card">
        <strong className="status">Backend ready to connect</strong>
        <p>Own-agent turn pipeline: browser audio → GPT transcription → GPT-5.4 Mini response → ElevenLabs TTS.</p>
      </section>
    </main>
  );
}
